# Large/generated files (excluded first)
*.fasta
*.fasta.*
data/*
results/*

# Essential data for Streamlit app (re-included)
!data/sequence_metadata.csv
!data/*.contree
!results/variant_frequencies.csv
!results/mutation_frequencies_per_position.csv
# all_mutations.csv is 1.1MB — skip unless you need per-sequence mutation explorer

# Browser/MCP session logs
.playwright-mcp/

# Embedded git repos (not submodules)
biotools-mcp-server/

# Python cache
__pycache__/
*.pyc
*.pyo

# One-off utility scripts (not part of pipeline)
fix_note*.py
rebuild_note.py
embed_images.py
update_note_with_images.py
build_report.py
create_docx.py
fruits.txt

# OpenCode internal
AGENTS.md

# OS files
Thumbs.db
.DS_Store

# Environment
.env
venv/
.venv/

# Batch output artifacts
KpNDM_*
tmp_*_tag.txt
M11CC_Out/

# OpenCode temp files
_opencode*
opencode*
C:*Users*
C:*UsershpAppDataLocalTempopencodestep3_current.txt
&#10;Users*

# One-off JSON artifacts
scripts/embed_images.json
scripts/small_tags.json
scripts/*.json

# Temp text files
fixed_note_content.txt
tmp_*.txt
